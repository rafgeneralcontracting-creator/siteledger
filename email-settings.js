(function(){
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
window.openEmailSettings=async function(){const rows=await rest('user_email_connections?select=id,provider,email_address,display_name,is_default&order=created_at.desc');modal(`<h2>Email Account</h2><div class="small" style="margin-bottom:14px">Connect the mailbox SiteLedger should use to send RFIs. The RFI PDF is sent as a real attachment.</div>${rows.map(r=>`<div class="card"><div class="title">${esc(r.email_address)}</div><div class="small">${esc(r.provider.toUpperCase())}${r.is_default?' · Default':''}</div></div>`).join('')||'<div class="notice">No sending mailbox connected yet.</div>'}<div class="section">Connect mailbox</div><div class="actions"><button class="btn secondary" onclick="showEmailProviderSetup('google')">Google / Gmail</button><button class="btn secondary" onclick="showEmailProviderSetup('microsoft')">Microsoft 365 / Outlook</button><button class="btn secondary" onclick="showEmailProviderSetup('smtp')">Other Email</button></div><div class="actions"><button class="btn secondary" onclick="closeModal()">Close</button></div>`)};
window.showEmailProviderSetup=function(provider){if(provider==='smtp'){modal(`<h2>Other Email (SMTP)</h2><div class="notice">Custom SMTP needs a secure server-side credential setup before it can send. We will never store the mailbox password in browser code.</div><div class="field"><label>Email address</label><input id="sl_mail_email" type="email"></div><div class="field"><label>SMTP host</label><input id="sl_mail_host" placeholder="smtp.example.com"></div><div class="field"><label>Port</label><input id="sl_mail_port" type="number" value="465"></div><div class="field"><label>Username</label><input id="sl_mail_user"></div><div class="field"><label>Password / app password</label><input id="sl_mail_pass" type="password"></div><div class="actions"><button class="btn secondary" onclick="closeModal()">Cancel</button><button class="btn primary" onclick="saveSmtpEmailConnection()">Save</button></div>`);return}modal(`<h2>${provider==='google'?'Google / Gmail':'Microsoft 365 / Outlook'}</h2><div class="notice">Direct OAuth authorization is the secure option for this provider. SiteLedger's connection screen is ready; the provider application credentials still need to be registered before Connect can be enabled.</div><div class="actions"><button class="btn secondary" onclick="closeModal()">Close</button></div>`)};
window.saveSmtpEmailConnection=async function(){const email=document.getElementById('sl_mail_email').value.trim(),host=document.getElementById('sl_mail_host').value.trim(),port=Number(document.getElementById('sl_mail_port').value),username=document.getElementById('sl_mail_user').value.trim(),password=document.getElementById('sl_mail_pass').value;if(!email||!host||!port||!username||!password)return alert('Complete all SMTP fields.');await rest('user_email_connections',{method:'POST',body:JSON.stringify({user_id:me.id,provider:'smtp',email_address:email,smtp_host:host,smtp_port:port,smtp_username:username,smtp_password:password,smtp_secure:true,is_default:true})});closeModal();alert('Email account saved. Direct SMTP sending will activate when the secure mail relay is configured.')};
})();